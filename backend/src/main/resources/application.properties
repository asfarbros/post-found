spring.application.name=backend

# # --- Supabase PostgreSQL Configuration (Using Pooler String Parts) ---
# # NOTE: Use the host from your pooler string
# spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require
# # Use the username from your pooler string
# spring.datasource.username=postgres.eenavlabqmuivuonnawk
# # Use the password from your pooler string (Make sure there are no extra spaces!)
# spring.datasource.password=ASFAR BROS 1975
# spring.datasource.driver-class-name=org.postgresql.Driver

# # --- JPA/Hibernate Configuration (Keep as is) ---
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# --- Supabase PostgreSQL Configuration ---

# The URL is constructed using the host from your DIRECT_URL
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require

# The username is extracted from your DIRECT_URL
spring.datasource.username=postgres.tjiadzqsloglmnqbrvyo

# Replace this with the password you created for this new 'postfound-v2' project
spring.datasource.password=ASFAR BROS 1975

spring.datasource.driver-class-name=org.postgresql.Driver

# --- Hikari Connection Pool Configuration ---
spring.datasource.hikari.maximumPoolSize=10

# --- JPA/Hibernate Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect




# --- JWT Secret (Keep as is) ---
jwt.secret=aVeryLongAndRandomSecretKeyThatNobodyCanGuess1234567890abcdefn

# --- Cloudinary Configuration ---
cloudinary.cloud_name=dzybsizjk
cloudinary.api_key=236474725187752
cloudinary.api_secret=oBk-67yPkI8BOwVNo7e2EHFjCTE

# --- Remove or comment out local upload settings ---
# file.upload-dir=./uploads
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB
```


    

